# Khai báo màu sắc
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
RED = (255, 0, 0)
BLUE = (0, 0, 255)
GREEN = (0, 255, 0)

# Cài đặt cửa sổ
WINDOW_SIZE = (800, 600)

node_radius = 10  # kích thước của các node
connection_distance = node_radius * 8 #Khoảng cách tối thiểu để xác định các node được kết nối với nhau
